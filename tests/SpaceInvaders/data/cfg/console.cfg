// in this case, the positions are relative to dynamic panel, but only if the dynamic panel is defined first
ui_dynamic_panel "main" "center" 0 0 800 600

// params:
// callEvent(str) - can be a cfg script or a function name
// themeIdentifier(str)
// positionRelativity(str: absolute, topright, bottomleft, bottomright, center, leftcenter, rightcenter, topcenter, bottomcenter )
// x(float), y(float), width(float), height(float)
// it means that it will move the x,y and then subtract enough to reach the relative choice. absolute means it doesn't subtract
ui_button "closeMain" "cross" "center" -100 -150 200 300
ui_button ui_console_send "rectangle" "bottomright" 796 596 246 26 // send button